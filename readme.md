**Roman numerals task implementation in PHP**

Generates the corresponding Roman numeral for a provided number using a TDD. 

For example: 1 should return "I", 6 should return "VI", and 10 should return "X".